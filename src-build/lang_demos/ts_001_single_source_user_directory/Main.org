#+STARTUP: showall

* TypeScript Single-Source User Directory

This example shows the single-source version of the JS plus =.d.ts= pipeline.

One file, =main.clj=, is used for both outputs:

- runtime JavaScript is compiled from the xtalk namespace with =:lang :js=
- declarations are generated from the same file through the mixer

* Generated build tree

Running =(make/build-all TS-001-SINGLE-SOURCE-USER-DIRECTORY)= writes:

- =.build/demo/ts-001-single-source-user-directory/.gitignore=
- =.build/demo/ts-001-single-source-user-directory/Makefile=
- =.build/demo/ts-001-single-source-user-directory/package.json=
- =.build/demo/ts-001-single-source-user-directory/src/index.js=
- =.build/demo/ts-001-single-source-user-directory/src/index.d.ts=

*** Build the project

#+BEGIN_SRC clojure :results output silent :cache no :eval no
(load-file "build.clj")
(require '[std.make :as make]
         '[lang-demos.ts-001-single-source-user-directory.build :as build])
(make/build-all build/TS-001-SINGLE-SOURCE-USER-DIRECTORY)
#+END_SRC

*** Inspect the generated declarations

#+BEGIN_SRC clojure :results output silent :cache no :eval no
(load-file "build.clj")
(require '[std.make :as make]
         '[lang-demos.ts-001-single-source-user-directory.build :as build])
(make/run build/TS-001-SINGLE-SOURCE-USER-DIRECTORY :show-types)
#+END_SRC

*** Inspect the generated JavaScript

#+BEGIN_SRC clojure :results output silent :cache no :eval no
(load-file "build.clj")
(require '[std.make :as make]
         '[lang-demos.ts-001-single-source-user-directory.build :as build])
(make/run build/TS-001-SINGLE-SOURCE-USER-DIRECTORY :show-js)
#+END_SRC
