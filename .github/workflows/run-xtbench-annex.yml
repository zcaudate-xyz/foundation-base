name: XTBench Annex

on:
  push:
    branches: [main, 'lang/php', 'lang/r']
  repository_dispatch:
  workflow_dispatch:

env:
  LEIN_JVM_OPTS: -Xms512m -Xmx3g -XX:+UseG1GC -XX:+HeapDumpOnOutOfMemoryError -XX:HeapDumpPath=/tmp
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

permissions:
  contents: write

jobs:
  run-xtbench-lang-tests:
    name: 01 - xt.lang / ${{ matrix.lang }}
    runs-on: ubuntu-latest
    timeout-minutes: 40
    strategy:
      fail-fast: false
      matrix:
        include:
          - lang: php
            image: ghcr.io/zcaudate-xyz/infra-foundation-xtbench-php:ci
          - lang: r
            image: ghcr.io/zcaudate-xyz/infra-foundation-xtbench-r:ci

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Log into GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ secrets.GH_USER }}
          password: ${{ secrets.GH_TOKEN }}

      - name: Run xt.lang
        run: >
          docker run --network host
          -e "LEIN_JVM_OPTS=${LEIN_JVM_OPTS}"
          -v /var/run/docker.sock:/var/run/docker.sock
          -v $(pwd):$(pwd)
          -w $(pwd)
          ${{ matrix.image }}
          lein seedgen test '[xt.lang]' :with "[${{ matrix.lang }}]" :metrics target/xtbench-metrics/result.json
      - name: Upload metrics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: xtbench-lang-${{ matrix.lang }}
          path: target/xtbench-metrics/result.json
          if-no-files-found: warn

  run-xtbench-event-tests:
    name: 02 - xt.event / php
    runs-on: ubuntu-latest
    timeout-minutes: 40

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Log into GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ secrets.GH_USER }}
          password: ${{ secrets.GH_TOKEN }}

      - name: Run xt.event
        run: >
          docker run --network host
          -e "LEIN_JVM_OPTS=${LEIN_JVM_OPTS}"
          -v /var/run/docker.sock:/var/run/docker.sock
          -v $(pwd):$(pwd)
          -w $(pwd)
          ghcr.io/zcaudate-xyz/infra-foundation-xtbench-php:ci
          lein seedgen test '[xt.event]' :with '[php]' :metrics target/xtbench-metrics/result.json
      - name: Upload metrics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: xtbench-event-php
          path: target/xtbench-metrics/result.json
          if-no-files-found: warn

  run-xtbench-kmi-tests:
    name: 10 - kmi.lang / php
    runs-on: ubuntu-latest
    timeout-minutes: 40

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Log into GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ secrets.GH_USER }}
          password: ${{ secrets.GH_TOKEN }}

      - name: Run kmi.lang
        run: >
          docker run --network host
          -e "LEIN_JVM_OPTS=${LEIN_JVM_OPTS}"
          -v /var/run/docker.sock:/var/run/docker.sock
          -v $(pwd):$(pwd)
          -w $(pwd)
          ghcr.io/zcaudate-xyz/infra-foundation-xtbench-php:ci
          lein seedgen test '[kmi.lang]' :with '[php]' :metrics target/xtbench-metrics/result.json
      - name: Upload metrics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: xtbench-kmi-php
          path: target/xtbench-metrics/result.json
          if-no-files-found: warn

  publish-metrics:
    name: Publish metrics
    if: always() && github.ref == 'refs/heads/main'
    needs: [run-xtbench-lang-tests, run-xtbench-event-tests, run-xtbench-kmi-tests]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/download-artifact@v4
        with:
          pattern: xtbench-*
          path: target/xtbench-artifacts
      - name: Publish metrics branch
        run: bash bin/publish-xtbench-metrics annex "$GITHUB_WORKSPACE/target/xtbench-artifacts" "$RUNNER_TEMP/xtbench-branch"
