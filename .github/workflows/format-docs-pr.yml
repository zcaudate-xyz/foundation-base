name: Format docs PR

on:
  pull_request:
    branches: [main]
    paths:
      - .github/workflows/format-docs-pr.yml

permissions:
  contents: read

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Export full repository workspace
        uses: actions/upload-artifact@v4
        with:
          name: foundation-workspace-full
          path: .
          include-hidden-files: true
